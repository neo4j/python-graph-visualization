# Changes in 1.3.0

## Breaking changes


## New features


## Bug fixes

* Fixed a bug with the theme detection. In VSCode the output would always be in dark theme as the reported background color was always reported as `rgba(0, 0, 0, 0)`. 

## Improvements

* Allow setting the theme manually in `VG.render(theme="light")` and `VG.render_widget(theme="dark")`.


## Other changes
